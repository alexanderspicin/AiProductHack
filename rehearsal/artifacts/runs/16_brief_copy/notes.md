# Обращение к участнику

Исправлено: «Ваша задача: понять причину сомнений клиента, связать решение с его задачами и договориться о следующем шаге».

Изменение только в TrainingBrief: удаляется начальный оборот «Сотрудник должен», подпись заканчивается двоеточием. Остальной текст и сохранённые сценарии не изменяются. Это не общий грамматический редактор; другие формулировки авторов сохраняются.

Проверка: `npm --prefix frontend test -- --run` (34 passed), `npm --prefix frontend run build` (успех), `git diff --check` (успех). HTTP 5176 отдаёт исправленный компонент. Осталось прежнее предупреждение о размере старого VoiceTools. API-вызовов нет. Решение keep.
