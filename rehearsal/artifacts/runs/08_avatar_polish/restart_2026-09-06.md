# Повторный запуск

Режим TASK2_AI. Дедлайн не задан, бюджет 5 минут. Запрос: снова открыть текущую лабораторию для ручного теста. Независимая проверка: порт 8897 не отвечает, нужен повторный запуск, не новая сборка. Код и библиотеку не меняем. GPU и платные API не нужны.

Команда из корня rehearsal:

```sh
artifacts/private/07_avatar_graph/cpu_env/bin/python artifacts/runs/07_avatar_graph/lab.py --root artifacts/private/07_avatar_graph --port 8897
```

Запуск вне песочницы нужен для macOS Milena: ранее внутри ограниченного окружения синтез возвращал тишину. Сервер привязан только к 127.0.0.1. Проверки после старта: доступность страницы, загрузка библиотеки и короткий локальный синтез.

Страница отвечает HTTP 200. Старая вкладка осталась на внутренней странице ошибки подключения; перезагрузка блокировалась для её data-адреса. Новая вкладка с исходным HTTP-адресом успешно загрузила библиотеку и показала «Готово. Введите фразу или используйте пример». Изменений приложения и платных вызовов нет.

Короткий запрос POST /speak: звук 3,337 с, подготовка 2,762 с, 84 кадра и 25 разных состояний рта. Проверка ненулевой речи прошла. Сервер оставлен работающим для пользователя.
