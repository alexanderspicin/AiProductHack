OPENAI_API_KEY=
# Must be a base64 "key:secret" Basic-auth token (Inworld Portal calls this the
# "Runtime API Key" — pipecat sends it verbatim as `Authorization: Basic <this>`).
# If you only have KEY and SECRET separately, base64-encode "KEY:SECRET" yourself.
INWORLD_API_KEY=
INWORLD_VOICE_ID=Svetlana
WHISPER_MODEL=medium
WHISPER_DEVICE=cuda
WHISPER_COMPUTE_TYPE=float16
